<!DOCTYPE html>
<html lang="en">
  <head>
    <title>WLRS - Workout: {{ workout_date }}</title>
  </head>
  <body>
    <div id="content">
      <p>Started: {{workout_start }}</p>
      <p>Ended: {{workout_end }}</p>
      {{#each items as |i|}}
        <p>{{ i }}</p>
      {{/each}}
    </div>
    <p>Comments: {{ comments }}</p>
  </body>
</html>